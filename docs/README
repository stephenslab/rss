### Preview the wiki website locally

- Run `script/bootstrap` to install the necessary dependencies
- Run `bundle exec jekyll serve` to start the preview server
- Visit http://localhost:4000/ to preview the website
